# 2026-03-18



## 知识库主题管理与新玩家雷达接入
- 修复知识库复制失败：为 navigator.clipboard.writeText 增加浏览器兼容兜底。
- 为知识库补充批量主题改名与整组删除接口，并在前端支持手动设置未归类主题、按主题整组删除。
- 为知识库页面增加焦点/定时自动刷新，减少综合报告生成后的手动刷新成本。
- 将 search_institutions 的工作流收敛为无参覆盖探测，并同步更新 people-intel API reference。
- 新增 fetch_latest_company_promotions 工具，把最近注册且近期被报道的公司信号接入 news-market 子 agent。
- 重写 market-intel 主 skill 文档为中文 UTF-8 版本，避免 Windows 环境下编码污染。
- 优化首页浅色主题可读性，提升统计卡片与新玩家雷达的文字对比度。

## 验证
- 后端：conda run -n cyber pytest -> 34 passed
- 前端：npm run build -> passed

## Skill 中文化、领域树接入与知识库稳定性修复
- 重写 people-intel、paper-analysis、market-intel 主 skill 文档为中文 UTF-8 干净版本，避免 Windows 环境继续出现编码污染。
- 重写 people-intel / paper-researcher 子 agent 提示词，明确 institutions 无参覆盖探测与 domains 领域树优先策略。
- 在 papers_tools 中新增 fetch_domain_tree()，默认调用 GET /domains?min_paper_count=0，用于赛道地图 / 知识图谱 / 赛道细分问题。
- 重写 people-intel 与 paper-analysis API reference，约束 institutions 默认无参调用，并补充 domains 使用说明。
- 为知识库存储返回增加文本与 metadata 递归清洗，降低脏数据导致 /deep/knowledge 500 的风险。
- 为知识库列表接口增加异常兜底日志。
- 重写前端 knowledge.ts，修复 navigator 未定义时 copyKnowledgeContent 报错，并保留 PDF Blob 导出方案。
- 新增 papers_tools 与 knowledge_store 回归测试，清洗 people_tools 测试文件编码。

## 验证
- 后端：conda run -n cyber pytest -> 37 passed
- 前端：npm run build -> passed

## 工具日志线程同步与知识库刷新风暴修复
- 重写 useChat，新增 gravity:active-thread 持久化，让工具日志页可以跟随当前聊天线程。
- 重写 ToolLogs 页面，修复日志视图与当前 thread 脱节的问题，并清理旧实现中的乱码与脆弱轮询。
- 重写 Knowledge 页面，拆分列表刷新与详情加载，避免详情页持续重复请求 /deep/knowledge、/deep/knowledge/categories 与详情接口。
- 保留知识库主题分组、手动改名、整组删除、复制全文、PDF 导出等能力，并让详情页只在显式打开时加载。

## 验证
- 前端：npm run build -> passed
- 后端：conda run -n cyber pytest -> 37 passed

## 知识库主题删除与 Tool Logs 循环刷新修复
- 后端将 /deep/knowledge/topic 与 /deep/knowledge/{item_id} 的匹配冲突收敛为强类型路径，修复主题整组删除时报 item_id 解析失败的 422。
- 重写 ToolLogs 页面加载链路，去掉 offset 触发的 reset 循环，避免页面持续重复请求 /deep/tool-logs?limit=50&offset=0。
- 保留当前聊天线程自动跟随逻辑，让工具日志页更容易切到最新 thread。

## 验证
- 前端：npm run build -> passed
- 后端：conda run -n cyber pytest -> 37 passed
