# Harness 运行资料

`harness/` 用来承接会持续演化的运行资料，而不是项目总说明。

目录说明：

- `rules/`：可演化规则与错误模式
- `tasks/`：任务定义
- `plans/`：执行计划
- `reports/`：阶段性输出与研究报告

规则：

- 通用经验优先放到 `rules/`
- 运行中产生的资料不要回填到总说明文档
- 总原则仍由 `docs/` 负责

