{
  "buildCommand": "cd quantum-engine && npm install && npm run build",
  "outputDirectory": "quantum-engine/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(?!api/).*", "destination": "/index.html" }
  ]
}
